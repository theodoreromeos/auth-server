# Server
server.port=0
server.servlet.context-path=/auth-server

# Issuer
issuer.url=http://localhost:9000/auth-server

# RSA Keys
jwt.rsa.private-key-path=classpath:keys/test-private_key.pem
jwt.rsa.public-key-path=classpath:keys/test-public_key.pem
jwt.rsa.key-id=test-key-id

# OAuth2 Client
oauth2.client.mobility-api.secret=test-secret
oauth2.redirect.uri=http://localhost:9999/login/callback
oauth2.redirect.logout.uri=http://localhost:9999/logout/callback

# Email token signing
jwt.signing.secret.key=dGhpcyBpcyBhIGJhc2U2NCBlbmNvZGVkIHRlc3Qgc2VjcmV0IGtleQ==
app.email-token-lifetime-seconds=86400

# Logging
logging.level.org.springframework.security=TRACE

# Disable gRPC for tests
#grpc.server.port=-1

# JPA
spring.jpa.hibernate.ddl-auto=none

admin.email=admin@mobilityapp.com
admin.password=p##s$worDd
admin.phone=00000000